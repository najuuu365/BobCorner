{
  "name": "haven-personal-workspace",
  "version": "1.0.0",
  "description": "Haven - A personal digital space, productivity hub, library, and mini-game arcade.",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "build": "npm run build:server && npm run build:client",
    "start": "cd server && npm start",
    "prisma:push": "cd server && npx prisma db push",
    "prisma:seed": "cd server && npx ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
